services:
  - type: web
    name: finance-streamlit
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run finance.py --server.port $PORT --server.address 0.0.0.0
